#Hello Everyone!

# 13.06.2026 First day
#
`code` This is my first linux project
`code` git commmit, init, pull, push, merge, branch, rebase, status, add
`code` ssh-keygen -t ed25519 -C "email" (create ssh key)
`code` sudo, cd, pwd, ls, mkdir, rm, touch(create file), echo, cat(read file), nano
`code` git remote learning (rate 3/10)

